#!/usr/bin/env bash
# .mise/tasks/chezmoi/dry-run
# Previews dotfile changes without applying them.
# Does not modify the filesystem.

set -eu

if [ -z "${CHEZMOI_SOURCE_DIR:-}" ]; then
  echo "CHEZMOI_SOURCE_DIR is not set."
  exit 1
fi

chezmoi apply --dry-run --source "$CHEZMOI_SOURCE_DIR"
